
import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const App = () => {
   return (
    <SafeAreaView>
      <Text>Hello React Native..</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  
});

export default App;
