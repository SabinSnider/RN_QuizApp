import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Quiz = (props) => {
  return <View>
      <Text>Wuiz</Text>
  </View>;
};

const styles = StyleSheet.create({});

export default Quiz;
